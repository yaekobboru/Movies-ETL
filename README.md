# Movies-ETL

This project create an automated pipeline that extract data from two sources , transform and load tod data base.
movie and data on different file formats such as csva nd json extracted from different sources and stored in a data frame as staging.
Then data transformed based on stated requiremetnt using regex also created a function to call calculations whenever required.
Finally merged data collected from different sources loaded into database.
I used PostGres SQL,python with different libraries to succedssfully excute the ETL.
